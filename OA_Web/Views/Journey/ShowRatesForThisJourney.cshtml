@model List<OA_DAL.Models.JourneyRate>

@{
    ViewData["Title"] = "Rates";
    
}




<div class="container">
    <partial name="_JourneyPartial" model="ViewBag.Journey" />

    <h2>Rates</h2>

    @foreach (var item in Model)
    {
        <div class="d-flex justify-content-between">
            <div class="d-flex align-content-center px-0">
                <img src="~/images/Users/@item.User.Photo" style="object-fit:cover" class="rounded-circle" width="40" height="40">
                <div class="comment-body">
                    <h6><a class="text-decoration-none text-dark" href="/Profile/Id/@item.User.Id">@item.User.FirstName @item.User.LastName</a></h6>
                    <p >@item.Comment</p>
                </div>

            </div>
            <div class=" px-0 pt-1">

                @for (int i = 1; i <= 5; i++)
                {
                    if (i <= item.Rate)
                    {


                        <span class="float-left fa fa-star checked"></span>
                    }
                    else
                    {
                        <span class="float-left fa fa-star"></span>
                    }
                }
                <div class="clearfix"></div>



            </div>
        </div>

        @*<div class="card" style="margin-top:15px;border:1px solid black">
            <div class="card-body">
                <div class="row">
                    <div class="col-md-2">
                        <img src="~/images/Users/@item.User.Photo" class="img img-rounded img-fluid" />
                    </div>
                    <div class="col-md-10">
                        <p>

                            @for (int i = 1; i <= 5; i++)
                            {
                                if (i <= item.Rate)
                                {


                                    <span class="float-right fa fa-star checked"></span>
                                }
                                else
                                {
                                    <span class="float-right fa fa-star"></span>
                                }
                            }
                            <a class="float-left" href="https://maniruzzaman-akash.blogspot.com/p/contact.html"><strong>@item.User.UserName</strong></a>


                        </p>
                        <div class="clearfix"></div>
                        <p>@item.Comment</p>

                    </div>
                </div>

            </div>
        </div>*@
    }
</div>

